{
  "name": "@artsband/api",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn api.index:app --reload --port 8000",
    "build": "python -m compileall api",
    "check": "python -m compileall api"
  }
}
